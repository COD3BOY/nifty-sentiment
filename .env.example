# ============================================================
# NIFTY Sentiment Prediction System - Environment Variables
# ============================================================
# Copy this file to .env and fill in your API keys.
#   cp .env.example .env
#
# Only ANTHROPIC_API_KEY is needed for news sentiment.
# The 3 yfinance sources (global markets, VIX, crude oil)
# work without any keys.
# ============================================================

# --- REQUIRED: Anthropic Claude API ---
# Get from: https://console.anthropic.com/ -> API Keys
# Enables: Zerodha Pulse news analysis, YouTube/Reddit sentiment
ANTHROPIC_API_KEY=

# --- OPTIONAL: Zerodha Kite Connect ---
# Get from: https://developers.kite.trade/ -> Create App
# Enables: GIFT Nifty pre-market gap (strongest predictor)
KITE_API_KEY=
KITE_API_SECRET=
# Access token expires daily. Generate via:
#   python -c "import sys; sys.path.insert(0,'.'); from utils.kite_auth import open_login; open_login()"
# Then exchange request_token:
#   python -c "import sys; sys.path.insert(0,'.'); from utils.kite_auth import generate_session; print(generate_session('REQUEST_TOKEN'))"
KITE_ACCESS_TOKEN=

# --- OPTIONAL: Reddit (PRAW) ---
# Get from: https://www.reddit.com/prefs/apps -> Create App (script type)
# Enables: r/IndianStreetBets sentiment
# Also install: pip install praw
REDDIT_CLIENT_ID=
REDDIT_CLIENT_SECRET=
REDDIT_USER_AGENT=nifty-sentiment-bot/1.0

# --- OPTIONAL: YouTube Data API ---
# Get from: https://console.cloud.google.com/ -> APIs -> YouTube Data API v3
# Enables: Financial advisor channel transcript analysis
# Also install: pip install youtube-transcript-api
YOUTUBE_API_KEY=
